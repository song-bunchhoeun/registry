import AutoScaleImage from './AutoScaleImage.js';

export default function MoEYSCertificateAppreciateYearly(props) {
    return <AutoScaleImage wrappedDocument={props.wrappedDocument}
    width={1280}
    height={901}
    maxWidth={1280} />; // will need to do actual size
}